using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Trap : MonoBehaviour {

	//Plant will pop up if the player gets close. Other plants will look similar to this enemy's plant.
	
	//Plant should be fairly big so the player can't just waltz past any grassy spot. OR have a lot of plants lol.
	
	//Check distance
	//If reached, play the animation
	//Set can be hurt to true.
	//Once animation finishes, set can hurt to be false.
	
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
