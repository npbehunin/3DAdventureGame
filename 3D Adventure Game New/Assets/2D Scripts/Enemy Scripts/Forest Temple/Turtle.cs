using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Turtle : MonoBehaviour {

	//Turtle walks slowly, has a lot of armor, especially against arrows.
	//Getting close causes the turtle to stomp the ground.
	
	//Check distance
	//If met, start coroutine for stomp
	//stops its movement and stomps the ground (animation plays)
	//Go back to normal
	
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
